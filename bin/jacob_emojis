#!/usr/bin/dash
selected="$(fuzzel --dmenu --placeholder="copy emoji" <$DOTDIR/dotconfig/hypr/emojis)"
[ -z "$selected" ] && exit
emoji="$(echo "$selected" | awk '{printf "%s", $NF}')"

wl-copy "$emoji"
wtype "$emoji"

notify-send -a "picker" -t 2000 -r 666 "Copied and typed $emoji"
