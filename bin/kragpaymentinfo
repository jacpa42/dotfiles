#!/usr/bin/env bash
info="$(fd -1gtfile "payment_info.txt" $PROJDIR)"
[ -z "$info" ] && {
    notify-send "failed to find 'payment_info.txt' in $PROJDIR :("
    exit
} || {
    wl-copy <$info
    notify-send -t 2000 "copied kragsentrale payment info to clipboard"
}
